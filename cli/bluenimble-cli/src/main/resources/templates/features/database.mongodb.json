{
	"name": "a name/id developers will use to work with this feature. Default to 'default'",
	
	"spec": {
		"cluster": "hosts where mongodb is running. Ex. host1:port1,host2:port2,...",
		"database": "database name",
		
		"auth": {
			"type": "sha",
			"user": "database username",
			"password": "password"
		}
	}
}
