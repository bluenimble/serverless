{
	
	"feature": "database",

	"spec": {
		"host": "where orientdb is running",
		"port": "orientdb port number",
		"database": "database name",
		
		"auth": {
			"user": "database username",
			"password": "password"
		}
	}
	
}
