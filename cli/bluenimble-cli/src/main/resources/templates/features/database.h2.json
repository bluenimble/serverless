{

	"feature": "database",

	"spec": {
		"vendor": "h2",
		"type": "memory or file",
		"database": "database name if type is file",
		
		"pool": {
			"maximumPoolSize": "10",
			"maxLifeTime": "30",
			"minimumIdle": "-1",
			"connectionTimeout": "30",
			"idleTimeout": "10"
		},
		
		"properties": {
		},
		
		"allowProprietaryAccess": "true"
	}
	
}
