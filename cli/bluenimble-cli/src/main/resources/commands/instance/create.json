{

	"handlers": {
		"space": {
			"description": "create a new space",
			
			"args": [{
				"name": "namespace", "desc": "space namespace"
			}],
		
			"request": {
				"method": "post",
				"service": "<% keys.endpoints.management %>/spaces/<% arg.0 %>"
			}
		}
	}

}