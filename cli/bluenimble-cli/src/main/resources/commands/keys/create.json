{

	"handlers": {
		"keys": {
			"description": "create new keys",

			"args": [{
				"name": "keys spec", "desc": "josn variable defining the keys. Example 'create keys specVar'. Use 'json create' command to create the specVar"
			}],
			
			"request": {
				"method": "post",
				"service": "[ keys.endpoint ]/keys",
				"contentType": "application/json",
				"body": {
					"payload": "[arg.0]"
				}
			}
		}
	}


}