{
	"handlers": {
		"entity": {
			"description": "drop an entity. Please use with caution, data is not recoverable",
			
			"args": [{
				"name": "entity", "desc": "database entity name"
			}],
		
			"request": {
				"method": "delete",
				"service": "[ keys.endpoints.management ]/db/[vars.database.provider]/entities/[arg.0]"
			}
		}
	}
}