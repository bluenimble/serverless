{

	"handlers": {
		"push": {
			"description": "get the status of a previous api push",
			
			"args": [{
				"name": "pushId", "desc": "the push id"
			}],
			
			"request": {
				"method": "get",
				"service": "[ keys.endpoints.management ]/apis/[arg.0]"
			}
		}
	}


}