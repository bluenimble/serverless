{

	"handlers": {
		"api": {
			"description": "start an api",
		
			"args": [{
				"name": "api", "desc": "namespace of the api to start"
			}],
			
			"request": {
				"method": "put",
				"service": "[ keys.endpoints.management ]/apis/[arg.0]/start"
			}
		}
	}

}