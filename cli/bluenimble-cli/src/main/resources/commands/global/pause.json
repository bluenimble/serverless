{

	"handlers": {
		"api": {
			"description": "pause an api",
		
			"args": [{
				"name": "api", "desc": "api namespace to pause"
			}],
		
			"request": {
				"method": "put",
				"service": "[ keys.endpoints.management ]/apis/[arg.0]"
			}
		}
	}

}