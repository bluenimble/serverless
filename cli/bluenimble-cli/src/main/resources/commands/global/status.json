{

	"handlers": {
		"api": {
			"description": "status of an api",
		
			"args": [{
				"name": "api", "desc": "namespace of the api"
			}],
			
			"request": {
				"method": "get",
				"service": "[ keys.endpoints.management ]/apis/[arg.0]/status"
			}
		}
	}

}