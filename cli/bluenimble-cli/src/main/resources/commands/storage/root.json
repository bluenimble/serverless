{

	"description": "get storage root",

	"request": {
		"method": "get",
		"service": "<% keys.endpoints.management %>/storage/<% vars.storage.provider %>/objects",
		"headers": {
			"Accept": "application/json"
		}
	}

}