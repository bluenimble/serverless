{

	"description": "get storage root",

	"request": {
		"method": "get",
		"service": "[ keys.endpoint ]/storage/[vars.storage.provider]/objects",
		"headers": {
			"Accept": "application/json"
		}
	}

}